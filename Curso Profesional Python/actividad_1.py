#Esta es una actividad en la que el usuario debe introducir los datos del triangulo y el programa debe mostrar como resultado el area de dicho triangulo.
print ("Introduce los datos del triangulo para hallar el area.")
print ("")
base = float(input("Base: "))
altura = float(input("Altura: "))
area = ((base*altura)/2)

print ("El area de tu triangulo es: "+ str(area))